Act as an expert Astro framework developer and a Senior SEO Specialist. I have a car AC repair and logistics platform in Riyadh called "Auto Save" (domain: autosaveks.com). I need to update and optimize my Astro project to dominate search engine results. 

Please provide the exact code, file structures, and content strategies for the following action items:

### 1. The "Knockout" Content Strategy (Niche SEO)
I have specific pages for car brands (Toyota, Hyundai, etc.). For these to rank, I need a standard template or actual ~200-word content blocks for the model pages. 
* Write an engaging, SEO-optimized 200-word text in Arabic targeting keywords like "أعطال مكيف تويوتا كامري في صيف الرياض" (AC issues for Toyota Camry in Riyadh summer). 
* Make it a template where I can easily swap the car brand and model.

### 2. Technical SEO & Sitemap Configuration in Astro
* Provide the exact configuration for `astro.config.mjs` using `@astrojs/sitemap`. I need it to automatically exclude any draft pages, test pages, or "coming soon" pages (e.g., using a filter for pages containing `/coming-soon`).
* Give me the best practice code for a `robots.txt` file tailored for an Astro site, ensuring it points to the sitemap.
* Show me how to set up a dynamic `<link rel="canonical" href={...} />` tag in my Astro Layout component to avoid duplicate content issues.

### 3. Rich Snippets (Schema Markup)
* Generate the JSON-LD Schema markup for a "LocalBusiness" or "AutoRepair" shop based in Riyadh. 
* Show me how to cleanly inject this JSON-LD script into the `<head>` of my Astro Layout or specific pages so Google can display star ratings, phone numbers, and location in search results.

### 4. Rendering & Performance Check
* I need to make sure my text content is directly in the source HTML and not relying on client-side hydration (so Google can read it instantly). Explain briefly how I should structure my Astro components (SSR vs SSG) to guarantee the text is baked into the static HTML.

Please provide the Arabic content for the cars first, followed by the Astro/JavaScript code blocks for the configurations!