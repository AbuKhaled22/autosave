---
/**
 * AC component/parts pages (Arabic — default locale).
 * Generates /parts/[component] for all 15 components.
 */
import { components } from '../../data/components';
import ComponentTemplate from '../../layouts/ComponentTemplate.astro';

export function getStaticPaths() {
  return components.map((component) => ({
    params: { component: component.slug },
    props: { component },
  }));
}

const { component } = Astro.props;
---

<ComponentTemplate component={component} lang="ar" />
